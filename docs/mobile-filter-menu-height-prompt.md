# Mobile Filter Menu Height Prompt

Update the mobile search filter drawer opened by the "other filters" button so it does not cover the full screen height. On mobile only, the drawer should be limited to 85% of the dynamic viewport height, leaving the bottom 15% visible. Keep the existing desktop filter layout unchanged, preserve the drawer header/footer, and ensure the filter list remains scrollable inside the capped drawer.
